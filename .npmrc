# Pin to the public registry.
# A global ~/.npmrc pointing at a private registry otherwise leaks an authenticated
# host into the lockfile, which then fails `npm ci` in CI.
registry=https://registry.npmjs.org/
